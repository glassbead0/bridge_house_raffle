<ul class="nav navbar-nav" id="dashboard" style="visibility: visible">
  <li class="active"><%= link_to 'Buy Tickets', ticket_new_wepay_path %></li>

  <% if current_admin %>
    <li><%= link_to 'All Tickets', tickets_path %></li>
    <li><%= link_to 'Set Payment Type', new_ticket_path %></li>
  <% end %>
</ul>

<ul class="nav navbar-nav navbar-right">
  <% if current_admin.event %>
    <li><%= link_to "End #{current_admin.event.name}", event_path(current_admin.event), method: 'delete', data: { confirm: 'Are you sure?' } %></li>
  <% else %>
    <li><%= link_to 'Start Event', new_event_path %></li>
  <% end %>
  <% if admin_signed_in? %>
    <li class="dropdown">
      <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_admin.email %> <span class="caret"></span></a>
      <ul class="dropdown-menu" role="menu">
        <li><%= link_to 'Logout', destroy_admin_session_path, method: 'delete' %></li>
        <li><%= link_to 'Account Settings', edit_admin_registration_path %></li>
      </ul>
    </li>
  <% end %>
</ul>

